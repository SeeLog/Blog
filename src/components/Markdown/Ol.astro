---
import { twMerge } from 'tailwind-merge';

interface Props {
  className?: string;
}

const props = Astro.props;

const cls = twMerge(
  props.className,
  '__md-ol',
)
---

<ol class={cls}>
  <slot />
</ol>
