---
import { twMerge } from 'tailwind-merge';

interface Props {
  className?: string;
}

const props = Astro.props;

const cls = twMerge(
  'my-4',
  props.className,
)
---

<a class="text-info hover:underline" {...props} target="_blank"><slot /></a>
