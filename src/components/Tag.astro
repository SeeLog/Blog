---
type Props = {
  href?: string
}

const { href } = Astro.props as Props;

const Container = href ? 'a' : 'div';
---

<Container class="badge badge-neutral px-3 py-3" href={href}>
  #<slot />
</Container>
